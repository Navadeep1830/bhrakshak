fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0.30,<2.1
asyncpg>=0.29
alembic>=1.13
pydantic>=2.7
pydantic-settings>=2.3
geoalchemy2>=0.15
shapely>=2.0
redis>=5.0
PyJWT>=2.8
bcrypt>=4.1
slowapi>=0.1.9
httpx>=0.27
python-multipart>=0.0.9
networkx>=3.3
pytest>=8.2
pytest-asyncio>=0.23

# --- ML inference stack -------------------------------------------------
# app/services/risk_engine.py imports numpy at module scope and loads the
# Model B bundle with joblib, so a deploy that skips these fails at import
# time and the API never binds a port. They were missing entirely.
numpy>=1.26,<3.0
joblib>=1.3
# Only needed when a trained bundle is present at
# app/services/ml_models/model_b_nowcast.pkl. Without them the risk engine
# logs a warning and falls back to its closed-form physical prior, which is a
# supported degraded mode -- so these are pinned loosely rather than required.
scikit-learn>=1.4
lightgbm>=4.3
xgboost>=2.0
