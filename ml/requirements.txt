numpy>=1.26
pandas>=2.2
# Every ingest cache in ml/cache/ is parquet. pandas does not bundle a parquet
# engine, so without this `ml.ingest.weather` pulls 20 years of history and
# then dies at save_cache() when it tries to merge with the existing file.
pyarrow>=14.0
geopandas>=0.14
shapely>=2.0
scikit-learn>=1.4
lightgbm>=4.3 ; python_version < "3.13"
xgboost>=2.0
# joblib serialises the model bundles that apps/api/app/services/risk_engine.py
# loads, and autoresearch/export_champion.py writes them.
joblib>=1.3
requests>=2.31
tqdm>=4.66
